---
layout: master
description: "Occasional notes and articles from Phillip Ridlen."
body_classes:
  - wide
styles:
  - posts/list
---

%h1#notes Notes

%section.posts
  %p.intro
    I don't write often or very much. Many of the following articles are barely
    more than links to other, better articles, and the rest are short and
    unpolished. Read at your own risk. ;)

  .notes-layout
    %main.posts-categories
      - posts_grouped_by_category.each do |category, posts|
        %section.posts-category
          %h3.header-link{ id: category }
            != link_to_category category
          .post-cards
            - posts.each do |post|
              != render "/post/card.html", post: post, compact: true
    %aside.notes-toc
      %nav{ "aria-labelledby": "notes-contents-heading" }
        %h3#notes-contents-heading Categories
        %ul
          - posts_grouped_by_category.each do |category, posts|
            %li
              %a{ href: "##{category}" }= category
              %small= posts.length
